name: Daily Automart Crawler

on:
  schedule:
    # 매일 오전 02:10 KST (UTC 17:10 전날)
    - cron: '10 17 * * *'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Backup CSV before crawl
        run: |
          if [ -f automart_master.csv ]; then
            cp automart_master.csv automart_master_backup.csv
          fi

      - name: Run crawler
        run: python crawler.py

      - name: Check for new Carnival KA4 and notify
        run: python notify_carnival.py

      - name: Cleanup backup
        run: rm -f automart_master_backup.csv

      - name: Commit and push CSV
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # 현재 날짜 (KST)
          DATE=$(TZ=Asia/Seoul date +%Y-%m-%d)

          git add automart_master.csv

          # 변경사항이 있을 때만 커밋
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update auction data: ${DATE}"
            git push
          fi
